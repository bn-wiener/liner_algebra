# 阶段 4 任务：正交、投影、最小二乘与 QR

阶段 3 已经解决了精确线性方程的结构问题：

- 怎样判断 $A\boldsymbol{x}=\boldsymbol{b}$ 是否有解；
- 怎样判断解是否唯一；
- 怎样求特解、零空间和全部解；
- 怎样理解秩和四个基本子空间；
- 怎样分解输入空间与输出空间。

阶段 4 要研究一个新的核心问题：

> 当目标 $\boldsymbol{b}$ 不在列空间中，精确方程 $A\boldsymbol{x}=\boldsymbol{b}$ 无解时，怎样找到距离目标最近的可实现输出？

本阶段的核心主题是：

$$
\text{内积与长度}
\longrightarrow
\text{正交}
\longrightarrow
\text{投影}
\longrightarrow
\text{最小二乘}
\longrightarrow
\text{Gram--Schmidt}
\longrightarrow
\text{QR 分解}.
$$

---

## 一、阶段 3 与阶段 4 的连接

阶段 3 研究的是：

$$
\boldsymbol{b}\in\operatorname{Col}(A)
$$

时，怎样求解：

$$
A\boldsymbol{x}=\boldsymbol{b}.
$$

但是现实数据通常带有噪声，多个约束也可能彼此不完全一致。这时：

$$
\boldsymbol{b}\notin\operatorname{Col}(A),
$$

原方程没有精确解。

阶段 4 不再强求矩阵产生无法达到的目标，而是在列空间中寻找离目标最近的向量：

$$
\boldsymbol{p}=A\widehat{\boldsymbol{x}}.
$$

这里：

- $\boldsymbol{p}$ 位于列空间中；
- $\boldsymbol{p}$ 是目标在列空间上的正交投影；
- $\widehat{\boldsymbol{x}}$ 是最小二乘解；
- 目标与投影之差称为残差。

定义残差：

$$
\boldsymbol{r}
=
\boldsymbol{b}-A\widehat{\boldsymbol{x}}.
$$

最关键的几何关系是：

$$
\boxed{
\boldsymbol{b}
=
\underbrace{A\widehat{\boldsymbol{x}}}_{\text{列空间分量}}
+
\underbrace{\boldsymbol{r}}_{\text{左零空间分量}}
}
$$

并且：

$$
\boldsymbol{r}
\perp
\operatorname{Col}(A).
$$

这正是阶段 3 中输出空间分解的实际应用：

$$
\mathbb R^m
=
\operatorname{Col}(A)
\oplus
\mathcal N(A^{\mathsf T}).
$$

---

## 二、第一步：内积、范数、距离与夹角

正交和投影都建立在内积之上，因此首先需要掌握内积所提供的几何测量。

重点学习：

- 向量内积；
- 欧几里得范数；
- 单位向量；
- 两个向量之间的距离；
- 两个非零向量之间的夹角；
- 柯西—施瓦茨不等式；
- 三角不等式。

需要理解：

$$
\boldsymbol{u}^{\mathsf T}\boldsymbol{v}
=
\|\boldsymbol{u}\|
\|\boldsymbol{v}\|
\cos\theta.
$$

内积不仅是一种代数运算，也在测量两个方向的一致程度：

- 内积为正，两个方向总体一致；
- 内积为零，两个方向正交；
- 内积为负，两个方向总体相反。

### 需要回答的问题

1. 内积为什么能够反映方向关系？
2. 范数和距离怎样由内积得到？
3. 为什么单位化只改变长度，不改变方向？
4. 夹角公式为什么需要同时除以两个向量的长度？
5. 柯西—施瓦茨不等式保证了什么？

---

## 三、第二步：正交、标准正交基与正交补

掌握内积以后，需要理解正交结构为什么比普通线性无关结构更便于计算。

重点学习：

- 两个向量正交；
- 正交向量组；
- 标准正交向量组；
- 标准正交基；
- 正交矩阵；
- 勾股定理；
- 子空间的正交补；
- 正交直和。

正交非零向量组一定线性无关，但线性无关向量组不一定正交。

若矩阵 $Q$ 的列是标准正交向量，则：

$$
Q^{\mathsf T}Q=I.
$$

标准正交基最重要的计算优势是：坐标可以直接通过内积得到，不需要重新解一个方程组。

本步还要重新理解阶段 3 的两组正交关系：

$$
\operatorname{Row}(A)
\perp
\mathcal N(A),
$$

$$
\operatorname{Col}(A)
\perp
\mathcal N(A^{\mathsf T}).
$$

### 需要回答的问题

1. 正交与线性无关有什么区别？
2. 为什么正交非零向量一定线性无关？
3. 什么是标准正交基？
4. 为什么标准正交基下的坐标可以直接用内积计算？
5. 什么是正交补？
6. 正交直和中的“直”和“正交”分别表示什么？

---

## 四、第三步：投影到直线和子空间

投影解决的问题是：

> 给定一个目标向量，怎样在指定子空间中找到离它最近的向量？

首先学习向量到一条直线的投影。若直线方向为非零向量 $\boldsymbol{a}$，则目标 $\boldsymbol{b}$ 的投影为：

$$
\operatorname{proj}_{\boldsymbol{a}}\boldsymbol{b}
=
\frac{
\boldsymbol{a}^{\mathsf T}\boldsymbol{b}
}{
\boldsymbol{a}^{\mathsf T}\boldsymbol{a}
}
\boldsymbol{a}.
$$

然后推广到具有标准正交基的子空间。

若 $Q$ 的列构成子空间的一组标准正交基，则：

$$
\boldsymbol{p}
=
QQ^{\mathsf T}\boldsymbol{b}.
$$

对应的投影矩阵为：

$$
P=QQ^{\mathsf T}.
$$

重点掌握：

- 投影点；
- 投影系数；
- 残差；
- 投影到直线；
- 投影到标准正交基张成的子空间；
- 投影矩阵；
- 投影矩阵的对称性与幂等性；
- 补空间投影。

投影矩阵应满足：

$$
P^{\mathsf T}=P,
$$

$$
P^2=P.
$$

### 需要回答的问题

1. 为什么最近点对应的残差必须与子空间正交？
2. 投影向量和残差分别属于哪个空间？
3. 为什么投影是唯一最近点？
4. 为什么投影两次与投影一次结果相同？
5. $P$ 与 $I-P$ 分别投影到哪个空间？

---

## 五、第四步：最小二乘与正规方程

当：

$$
A\boldsymbol{x}=\boldsymbol{b}
$$

没有精确解时，寻找使误差最小的参数：

$$
\widehat{\boldsymbol{x}}
=
\underset{\boldsymbol{x}}{\operatorname{argmin}}
\|A\boldsymbol{x}-\boldsymbol{b}\|^2.
$$

所有可实现输出 $A\boldsymbol{x}$ 构成列空间，因此最小二乘是在列空间中寻找距离 $\boldsymbol{b}$ 最近的向量。

最佳残差满足：

$$
A^{\mathsf T}
\left(
\boldsymbol{b}-A\widehat{\boldsymbol{x}}
\right)
=
\boldsymbol{0}.
$$

由此得到正规方程：

$$
\boxed{
A^{\mathsf T}A\widehat{\boldsymbol{x}}
=
A^{\mathsf T}\boldsymbol{b}
}
$$

重点掌握：

- 精确解与最小二乘解的区别；
- 最小二乘目标函数；
- 投影点与最小二乘参数；
- 残差正交条件；
- 正规方程的几何推导；
- 正规方程的代数推导；
- 最小二乘解的存在性与唯一性；
- 投影点唯一与参数可能不唯一的区别。

### 需要回答的问题

1. 最小二乘究竟在最小化什么？
2. 为什么最佳近似是列空间上的投影？
3. 为什么残差与矩阵的每一列正交？
4. 正规方程为什么一定有解？
5. 什么时候最小二乘参数唯一？
6. 为什么投影输出唯一，但产生它的参数可能不唯一？

---

## 六、第五步：Gram–Schmidt 正交化

普通的线性无关基可以描述子空间，但投影计算并不方便。Gram–Schmidt 方法把普通基转化为标准正交基，同时保持张成空间不变。

设原始向量为：

$$
\boldsymbol{a}_1,
\boldsymbol{a}_2,
\ldots,
\boldsymbol{a}_n.
$$

基本思想是：

1. 保留第一个方向并单位化；
2. 从第二个向量中减去它在已有方向上的投影；
3. 对剩余向量重复这个过程；
4. 最终得到一组标准正交向量。

重点掌握：

- 正交化与单位化的区别；
- 每一步减去已有方向的投影；
- 为什么新向量与已有方向正交；
- 为什么张成空间保持不变；
- 经典 Gram–Schmidt；
- 改进 Gram–Schmidt；
- 线性相关时算法怎样暴露零向量。

### 需要回答的问题

1. Gram–Schmidt 每一步减去的是什么？
2. 为什么减去投影后会得到正交方向？
3. 为什么正交化不会改变原来的列空间？
4. 为什么最后还要单位化？
5. 原向量线性相关时会发生什么？

---

## 七、第六步：QR 分解

将 Gram–Schmidt 的结果组织成矩阵，就得到 QR 分解：

$$
A=QR.
$$

其中：

- $Q$ 的列是标准正交向量；
- $R$ 是上三角矩阵；
- $Q$ 与 $A$ 具有相同的列空间；
- $R$ 记录原列向量在新标准正交基下的坐标。

重点掌握：

- 经济型 QR 分解；
- 完整 QR 分解；
- $Q$ 与 $R$ 的维度；
- $Q^{\mathsf T}Q=I$；
- $R$ 为什么是上三角矩阵；
- 怎样验证 $A=QR$；
- 怎样利用 QR 求解方程；
- 怎样利用 QR 求最小二乘解。

若 $A$ 满列秩，最小二乘问题可以化为：

$$
\boxed{
R\widehat{\boldsymbol{x}}
=
Q^{\mathsf T}\boldsymbol{b}
}
$$

因为 $R$ 是上三角矩阵，可以通过回代求解。

### 需要回答的问题

1. $Q$ 和 $R$ 分别记录什么信息？
2. 为什么 $Q$ 与 $A$ 的列空间相同？
3. 为什么 $R$ 是上三角矩阵？
4. 经济型 QR 与完整 QR 有什么区别？
5. 为什么 QR 能把最小二乘问题化成上三角方程？
6. 为什么数值计算中通常优先使用 QR，而不是显式求逆？

---

## 八、第七步：数据拟合与最小二乘应用

完成理论结构后，需要用数据拟合把整个阶段连接起来。

首先学习直线拟合：

$$
y=\beta_0+\beta_1x.
$$

对多个观测点建立设计矩阵：

$$
A\boldsymbol{\beta}\approx\boldsymbol{b}.
$$

然后求出使残差平方和最小的参数。

进一步理解：

- 每一行对应一个观测样本；
- 每一列对应一个模型基函数；
- 列空间表示模型能够产生的全部预测；
- 投影点表示最佳预测；
- 残差表示观测值中模型无法解释的部分。

重点掌握：

- 直线拟合；
- 多项式拟合；
- 设计矩阵；
- 预测向量；
- 残差向量；
- 残差平方和；
- 参数唯一性；
- 特征列接近相关时的数值问题。

### 需要回答的问题

1. 怎样把一组数据写成矩阵模型？
2. 设计矩阵的行和列分别表示什么？
3. 为什么最佳拟合等价于正交投影？
4. 残差为什么包含模型无法解释的信息？
5. 多项式模型为什么仍然可以是线性最小二乘？
6. 为什么特征列接近相关时参数会不稳定？

---

## 九、数值计算中需要建立的意识

阶段 4 不仅要理解数学公式，还要初步建立数值稳定性意识。

理论上，满列秩时可以写成：

$$
\widehat{\boldsymbol{x}}
=
(A^{\mathsf T}A)^{-1}
A^{\mathsf T}\boldsymbol{b}.
$$

但实际计算中不应优先显式求逆。

需要理解：

- 求逆公式适合理论推导，不是首选计算方法；
- 直接构造 $A^{\mathsf T}A$ 会放大条件问题；
- QR 分解通常比正规方程更稳定；
- 经典 Gram–Schmidt 在浮点计算中可能失去正交性；
- 改进 Gram–Schmidt、Householder 反射和 Givens 旋转更适合数值计算；
- 当矩阵接近秩亏时，后续需要使用 SVD 和伪逆。

本阶段只需建立这些意识，不要求立即深入所有数值算法。

---

## 十、推荐学习顺序

按照以下顺序推进：

```text
内积、范数、距离与夹角
→ 正交与标准正交基
→ 正交补与正交分解
→ 投影到直线
→ 投影到子空间
→ 投影矩阵与残差
→ 最小二乘与正规方程
→ Gram–Schmidt
→ QR 分解
→ 使用 QR 求最小二乘
→ 直线拟合与多项式拟合
```

暂时不进入：

- 行列式；
- 特征值与特征向量；
- 对角化；
- 动态系统；
- SVD 与伪逆。

这些内容将在后续阶段分别处理。

---

## 十一、阶段完成标准

阶段 4 完成时，应当能够拿到一个简单矩阵 $A$ 和目标向量 $\boldsymbol{b}$，独立回答：

1. 怎样计算向量的内积、长度、距离和夹角？
2. 两个向量是否正交？一组向量是否标准正交？
3. 某个子空间的正交补是什么？
4. 怎样把向量投影到一条直线？
5. 怎样把向量投影到一个具有标准正交基的子空间？
6. 投影向量和残差分别位于哪个空间？
7. 为什么投影点是唯一最近点？
8. 投影矩阵为什么满足对称性和幂等性？
9. 当 $A\boldsymbol{x}=\boldsymbol{b}$ 无解时，怎样建立最小二乘问题？
10. 怎样从残差正交条件推导正规方程？
11. 最小二乘投影点和最小二乘参数是否唯一？
12. 怎样使用 Gram–Schmidt 构造标准正交基？
13. 怎样计算并验证经济型 QR 分解？
14. 怎样利用 QR 分解求最小二乘解？
15. 怎样从观测数据建立直线或多项式拟合模型？
16. 为什么实际计算通常优先使用 QR，而不是显式求逆？

完成本阶段后，应形成下面的统一认识：

$$
\boxed{
\begin{aligned}
\text{投影}
&:\text{寻找子空间中的最近点},\\
\text{最小二乘}
&:\text{把目标投影到矩阵的列空间},\\
\text{Gram--Schmidt}
&:\text{为列空间构造标准正交基},\\
\text{QR}
&:\text{用标准正交坐标稳定地完成投影与求解}.
\end{aligned}
}
$$

阶段 4 完成后，最自然的下一阶段是：

$$
\text{行列式}
\longrightarrow
\text{特征值与特征向量}
\longrightarrow
\text{对角化}
\longrightarrow
\text{动态系统}.
$$

因为下一阶段研究的问题将从“怎样找到最近的可实现输出”转向：

> 在线性变换反复作用时，哪些方向保持不变，哪些方向会被拉伸、压缩或翻转？
