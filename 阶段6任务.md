# 阶段 6 任务：SVD、伪逆、低秩近似与数据分析

阶段 5 已经解决了“方阵内部有哪些不变方向，以及这些方向怎样决定矩阵幂和动态系统”的问题：

- 使用行列式判断体积缩放与可逆性；
- 使用特征值和特征向量寻找不变方向；
- 使用特征空间区分独立变化模式；
- 使用对角化把复杂方阵化成独立缩放；
- 使用谱定理正交对角化实对称矩阵；
- 使用特征值的幂分析矩阵幂和长期动态。

但特征分解存在明显限制：

- 非方阵不能研究 $Av=\lambda v$；
- 方阵也可能没有足够多的特征向量；
- 一般特征向量不一定正交；
- 秩亏矩阵和病态矩阵的稳定求解仍需更统一的工具；
- 数据矩阵通常是矩形的，并且往往只在少数方向上包含主要信息。

阶段 6 要研究一个更一般的核心问题：

> 对任意矩阵，哪些输入方向最重要？它们分别被映射到哪些输出方向？矩阵真正保留了多少信息，又怎样稳定地求解、压缩和分析这些信息？

本阶段的核心主题是：

$$
\text{奇异值与奇异向量}
\longrightarrow
\text{SVD}
\longrightarrow
\text{秩与四个基本子空间}
\longrightarrow
\text{伪逆}
\longrightarrow
\text{条件数与稳定性}
\longrightarrow
\text{截断 SVD}
\longrightarrow
\text{低秩近似}
\longrightarrow
\text{PCA}.
$$

---

## 一、阶段 5 与阶段 6 的连接

阶段 5 的特征值问题要求：

$$
A\in\mathbb R^{n\times n},
$$

并寻找：

$$
Av=\lambda v.
$$

这里输入 $v$ 与输出 $Av$ 必须位于同一个空间，特征向量要求输出仍留在输入方向上。

阶段 6 研究任意矩阵：

$$
A:\mathbb R^n\longrightarrow\mathbb R^m.
$$

当 $m\neq n$ 时，输入与输出甚至不在同一个空间，所以不能要求它们方向相同。SVD 改为寻找两组正交方向：

$$
\boxed{
Av_i=\sigma_i u_i
},
$$

其中：

- $v_i\in\mathbb R^n$ 是输入空间中的右奇异向量；
- $u_i\in\mathbb R^m$ 是输出空间中的左奇异向量；
- $\sigma_i\geq0$ 是对应的奇异值。

SVD 的完整形式为：

$$
\boxed{
A=U\Sigma V^{\mathsf T}
}.
$$

它把任意线性变换统一解释为：

```text
输入向量 x
   │
   │ Vᵀ：转换到输入空间的右奇异向量坐标
   ▼
输入主方向坐标
   │
   │ Σ：沿各方向按非负奇异值缩放，并处理维数变化
   ▼
输出主方向坐标
   │
   │ U：转换到输出空间
   ▼
输出 Ax
```

阶段 5 的谱定理是阶段 6 的理论基础，因为：

$$
A^{\mathsf T}A
$$

和：

$$
AA^{\mathsf T}
$$

都是实对称半正定矩阵，可以正交对角化。

阶段 6 的最终目标是理解并能够使用：

$$
\boxed{
A
=
\sum_{i=1}^{r}
\sigma_i u_i v_i^{\mathsf T}
},
$$

以及：

$$
\boxed{
A^+
=
V_r\Sigma_r^{-1}U_r^{\mathsf T}
},
$$

并进一步用前 $k$ 个奇异方向构造最佳低秩近似和 PCA。

---

## 二、第一步：从 $A^{\mathsf T}A$ 到奇异值与奇异向量

对任意：

$$
A\in\mathbb R^{m\times n},
$$

矩阵：

$$
A^{\mathsf T}A\in\mathbb R^{n\times n}
$$

是实对称矩阵，因为：

$$
(A^{\mathsf T}A)^{\mathsf T}
=A^{\mathsf T}A.
$$

它还是半正定矩阵，因为对任意 $x\in\mathbb R^n$：

$$
x^{\mathsf T}A^{\mathsf T}Ax
=
\|Ax\|^2
\geq0.
$$

因此 $A^{\mathsf T}A$ 的所有特征值都为非负实数，并且存在标准正交特征向量基：

$$
A^{\mathsf T}Av_i
=
\sigma_i^2v_i.
$$

定义奇异值：

$$
\boxed{
\sigma_i
=
\sqrt{\lambda_i(A^{\mathsf T}A)}
}.
$$

通常按降序排列：

$$
\sigma_1
\geq
\sigma_2
\geq
\cdots
\geq
\sigma_p
\geq0,
\qquad
p=\min(m,n).
$$

$A^{\mathsf T}A$ 的标准正交特征向量 $v_i$ 称为右奇异向量。

当 $\sigma_i>0$ 时，定义：

$$
\boxed{
u_i
=
\frac{Av_i}{\sigma_i}
}.
$$

于是：

$$
Av_i=\sigma_i u_i,
$$

并且：

$$
A^{\mathsf T}u_i=\sigma_i v_i.
$$

$u_i$ 是 $AA^{\mathsf T}$ 的标准正交特征向量：

$$
AA^{\mathsf T}u_i
=
\sigma_i^2u_i.
$$

重点学习：

- 为什么 $A^{\mathsf T}A$ 一定对称半正定；
- 为什么它的特征值不会为负；
- 奇异值为什么取特征值的非负平方根；
- 右奇异向量为什么位于输入空间；
- 左奇异向量为什么位于输出空间；
- 为什么非零奇异值对应的 $u_i$ 自动标准正交；
- $A^{\mathsf T}A$ 与 $AA^{\mathsf T}$ 为什么具有相同的非零特征值；
- 零奇异值为什么对应被矩阵完全消除的输入方向。

需要特别理解：

$$
\sigma_i=0
\Longleftrightarrow
Av_i=0
\Longleftrightarrow
v_i\in\mathcal N(A).
$$

### 需要回答的问题

1. 为什么任意矩阵都可以通过 $A^{\mathsf T}A$ 进入对称矩阵理论？
2. 为什么 $A^{\mathsf T}A$ 的特征值一定非负？
3. 奇异值为什么是特征值的平方根，而不是特征值本身？
4. 左奇异向量和右奇异向量分别属于哪个空间？
5. 为什么 $u_i=Av_i/\sigma_i$ 只适用于 $\sigma_i>0$？
6. 零奇异值怎样揭示矩阵丢失的信息方向？

---

## 三、第二步：SVD 的形式、计算与几何意义

完整 SVD 写成：

$$
\boxed{
A=U\Sigma V^{\mathsf T}
}.
$$

其中：

$$
U\in\mathbb R^{m\times m},
\qquad
V\in\mathbb R^{n\times n}
$$

都是正交矩阵，$\Sigma\in\mathbb R^{m\times n}$ 是矩形对角矩阵。

需要区分三种常见形式。

### 完整 SVD

保留输入空间与输出空间的完整标准正交基：

$$
A=U\Sigma V^{\mathsf T}.
$$

### 经济型 SVD

令 $p=\min(m,n)$，只保留 $p$ 个可能参与矩形对角结构的方向：

$$
A=U_p\Sigma_pV_p^{\mathsf T},
$$

其中：

$$
U_p\in\mathbb R^{m\times p},
\qquad
\Sigma_p\in\mathbb R^{p\times p},
\qquad
V_p\in\mathbb R^{n\times p}.
$$

### 紧致 SVD

若：

$$
r=\operatorname{rank}(A),
$$

只保留 $r$ 个正奇异值：

$$
\boxed{
A=U_r\Sigma_rV_r^{\mathsf T}
}.
$$

其中：

$$
U_r\in\mathbb R^{m\times r},
\qquad
\Sigma_r\in\mathbb R^{r\times r},
\qquad
V_r\in\mathbb R^{n\times r}.
$$

SVD 的几何过程是：

$$
x
\xrightarrow{V^{\mathsf T}}
V^{\mathsf T}x
\xrightarrow{\Sigma}
\Sigma V^{\mathsf T}x
\xrightarrow{U}
Ax.
$$

- $V^{\mathsf T}$ 在输入空间中旋转或反射；
- $\Sigma$ 沿正交坐标轴按 $\sigma_i$ 缩放，并允许维数变化；
- $U$ 在输出空间中旋转或反射。

输入单位球经过 $A$ 后变成椭球：

- 输入主方向是 $v_i$；
- 输出主轴方向是 $u_i$；
- 椭球半轴长度是 $\sigma_i$；
- 零奇异值把某些方向压到零。

手算时采用以下流程：

1. 计算 $A^{\mathsf T}A$ 或 $AA^{\mathsf T}$；
2. 求特征值和标准正交特征向量；
3. 对特征值开平方并降序排列，得到奇异值；
4. 计算非零奇异值对应的另一侧奇异向量；
5. 必要时补齐零空间中的标准正交基；
6. 组织 $U,\Sigma,V$；
7. 验证维数、正交性与 $A=U\Sigma V^{\mathsf T}$。

建议使用贯穿矩阵：

$$
A=
\begin{bmatrix}
1&1\\
1&1\\
0&0
\end{bmatrix}
$$

完成一次完整、经济型和紧致 SVD，并解释为什么它的秩为 $1$。

### 需要回答的问题

1. 完整 SVD、经济型 SVD 和紧致 SVD 的维数分别是什么？
2. 为什么紧致 SVD 只保留正奇异值？
3. $V^{\mathsf T}$、$\Sigma$ 和 $U$ 分别完成什么几何作用？
4. 为什么单位球经过矩阵后变成以 $u_i$ 为主轴的椭球？
5. 手算时怎样在 $A^{\mathsf T}A$ 与 $AA^{\mathsf T}$ 之间选择？
6. SVD 的哪些部分不唯一，哪些信息是确定的？

---

## 四、第三步：SVD、秩、四个基本子空间与外积展开

若奇异值按降序排列，并且：

$$
\sigma_1\geq\cdots\geq\sigma_r>0,
$$

其余奇异值为零，则：

$$
\boxed{
\operatorname{rank}(A)=r
}.
$$

SVD 可以直接给出四个基本子空间：

$$
\boxed{
\operatorname{Col}(A)
=
\operatorname{span}
\{u_1,\ldots,u_r\}
},
$$

$$
\boxed{
\mathcal N(A^{\mathsf T})
=
\operatorname{span}
\{u_{r+1},\ldots,u_m\}
},
$$

$$
\boxed{
\operatorname{Row}(A)
=
\operatorname{Col}(A^{\mathsf T})
=
\operatorname{span}
\{v_1,\ldots,v_r\}
},
$$

$$
\boxed{
\mathcal N(A)
=
\operatorname{span}
\{v_{r+1},\ldots,v_n\}
}.
$$

完整 SVD 因而把两个空间分别正交分解为：

$$
\mathbb R^m
=
\operatorname{Col}(A)
\oplus
\mathcal N(A^{\mathsf T}),
$$

$$
\mathbb R^n
=
\operatorname{Row}(A)
\oplus
\mathcal N(A).
$$

紧致 SVD 还可以展开为：

$$
\boxed{
A
=
\sigma_1u_1v_1^{\mathsf T}
+\cdots+
\sigma_ru_rv_r^{\mathsf T}
}.
$$

每个：

$$
\sigma_iu_iv_i^{\mathsf T}
$$

都是秩一矩阵，表示：

1. 用 $v_i^{\mathsf T}x$ 提取输入在 $v_i$ 方向上的分量；
2. 将该分量乘以 $\sigma_i$；
3. 沿输出方向 $u_i$ 产生结果。

重点学习：

- 秩为什么等于正奇异值数量；
- 左奇异向量怎样分解输出空间；
- 右奇异向量怎样分解输入空间；
- 零奇异值与零空间的关系；
- 外积为什么产生秩一矩阵；
- 为什么 SVD 是若干独立秩一作用的叠加；
- 精确秩与数值秩的区别。

数值秩不应只通过“奇异值是否严格等于零”判断，而应结合尺度和容差识别哪些奇异值相对于 $\sigma_1$ 足够小。

### 需要回答的问题

1. 为什么正奇异值的数量等于矩阵的秩？
2. 哪些左奇异向量张成列空间和左零空间？
3. 哪些右奇异向量张成行空间和零空间？
4. 为什么 $u_iv_i^{\mathsf T}$ 的秩至多为 $1$？
5. 外积展开怎样解释矩阵对任意输入向量的作用？
6. 精确秩和数值秩为什么可能不同？

---

## 五、第四步：Moore–Penrose 伪逆、投影与解问题

普通逆矩阵只适用于可逆方阵。阶段 6 需要为矩形矩阵、秩亏矩阵和不一致方程建立统一的“逆作用”。

若紧致 SVD 为：

$$
A=U_r\Sigma_rV_r^{\mathsf T},
$$

则 Moore–Penrose 伪逆定义为：

$$
\boxed{
A^+
=
V_r\Sigma_r^{-1}U_r^{\mathsf T}
}.
$$

它的含义是：

- 沿左奇异方向提取输出坐标；
- 对每个正奇异值做 $1/\sigma_i$ 的反向缩放；
- 转回右奇异向量所在的输入空间；
- 对已经丢失的零奇异值方向不做虚假的恢复。

伪逆满足 Moore–Penrose 四个条件：

$$
AA^+A=A,
$$

$$
A^+AA^+=A^+,
$$

$$
(AA^+)^{\mathsf T}=AA^+,
$$

$$
(A^+A)^{\mathsf T}=A^+A.
$$

两个重要乘积具有投影意义：

$$
\boxed{
AA^+
=
U_rU_r^{\mathsf T}
},
$$

它是投影到 $\operatorname{Col}(A)$ 的正交投影矩阵；

$$
\boxed{
A^+A
=
V_rV_r^{\mathsf T}
},
$$

它是投影到 $\operatorname{Row}(A)$ 的正交投影矩阵。

对任意方程：

$$
Ax=b,
$$

伪逆给出：

$$
\boxed{
x^+=A^+b
}.
$$

它统一表示：

- 可逆方阵的唯一精确解；
- 满列秩高矩阵的唯一最小二乘解；
- 满行秩宽矩阵的最小范数精确解；
- 一般秩亏矩阵的最小范数最小二乘解。

所有最小二乘解可以写成：

$$
x
=
A^+b
+(I-A^+A)z,
$$

其中第二项位于 $\mathcal N(A)$。$A^+b$ 与零空间正交，因此在所有最小二乘解中范数最小。

### 需要回答的问题

1. 为什么普通逆矩阵不能处理矩形或秩亏矩阵？
2. 伪逆为什么只对正奇异值取倒数？
3. $AA^+$ 和 $A^+A$ 分别投影到哪个空间？
4. 为什么 $A^+b$ 总能给出最小二乘解？
5. 当精确解不唯一时，为什么 $A^+b$ 是最小范数解？
6. 可逆方阵情况下为什么 $A^+=A^{-1}$？

---

## 六、第五步：奇异值、矩阵范数、条件数与数值敏感性

最大奇异值给出矩阵对单位向量的最大放大倍数：

$$
\boxed{
\|A\|_2
=
\sigma_1
}.
$$

若 $A$ 在输入空间中没有零方向，则最小奇异值描述最弱的放大倍数：

$$
\sigma_{\min}
=
\min_{\|x\|=1}\|Ax\|.
$$

Frobenius 范数满足：

$$
\boxed{
\|A\|_F
=
\sqrt{
\sigma_1^2+\cdots+\sigma_r^2
}
}.
$$

对于满秩方阵，二范数条件数为：

$$
\boxed{
\kappa_2(A)
=
\frac{\sigma_1}{\sigma_n}
}.
$$

若矩阵秩亏，则存在零奇异值，通常把条件数视为无穷大。

条件数的几何意义是最大轴长与最小轴长之比：

$$
\kappa_2(A)
=
\frac{\text{最大伸缩}}{\text{最小伸缩}}.
$$

小奇异值方向十分危险。若：

$$
b
=
\sum_i\beta_i u_i,
$$

伪逆解为：

$$
A^+b
=
\sum_{i=1}^{r}
\frac{\beta_i}{\sigma_i}v_i.
$$

当 $\sigma_i$ 很小时，输出或数据中的微小误差会被 $1/\sigma_i$ 大幅放大。

重点学习：

- 谱范数为什么等于最大奇异值；
- 最小奇异值为什么测量接近不可逆的程度；
- Frobenius 范数怎样由全部奇异值组成；
- 条件数为什么比较最大与最小伸缩；
- 秩亏为什么意味着条件数无穷大；
- 小奇异值怎样放大测量噪声和舍入误差；
- 代数上可逆为什么不一定意味着数值上容易求逆。

### 需要回答的问题

1. 为什么 $\sigma_1$ 是矩阵的最大放大倍数？
2. 最小奇异值接近零在几何上表示什么？
3. 条件数大为什么意味着问题对扰动敏感？
4. 为什么秩亏矩阵的条件数被视为无穷大？
5. 小奇异值为什么会在伪逆中放大噪声？
6. 谱范数与 Frobenius 范数分别汇总了哪些奇异值信息？

---

## 七、第六步：截断 SVD、最佳低秩近似、压缩与去噪

由外积展开：

$$
A
=
\sum_{i=1}^{r}
\sigma_i u_i v_i^{\mathsf T}.
$$

若只保留前 $k$ 个最大奇异值及其方向：

$$
\boxed{
A_k
=
\sum_{i=1}^{k}
\sigma_i u_i v_i^{\mathsf T}
},
\qquad
k<r,
$$

就得到截断 SVD。

$A_k$ 的秩不超过 $k$，却保留了矩阵中最强的 $k$ 个正交作用方向。

Eckart–Young 定理说明，$A_k$ 是最优低秩近似：

$$
\boxed{
\min_{\operatorname{rank}(B)\leq k}
\|A-B\|_2
=
\sigma_{k+1}
},
$$

并且：

$$
\boxed{
\min_{\operatorname{rank}(B)\leq k}
\|A-B\|_F
=
\sqrt{
\sum_{i=k+1}^{r}\sigma_i^2
}
}.
$$

低秩表示的存储量约为：

$$
k(m+n+1),
$$

而原矩阵需要：

$$
mn
$$

个数。当 $k\ll\min(m,n)$ 时，可以显著压缩数据。

需要理解以下应用：

- 图像压缩：保留主要空间模式；
- 去噪：丢弃弱小、可能由噪声主导的方向；
- 潜在结构发现：用少数秩一模式解释数据；
- 截断伪逆：不对极小奇异值取倒数，从而稳定求解；
- 降维：把高维对象投影到主要奇异方向。

同时要建立边界意识：

- 小奇异值不一定都是噪声；
- 重要性取决于任务，不只取决于重建误差；
- Eckart–Young 最优性针对谱范数和 Frobenius 范数；
- 选择 $k$ 是压缩、误差与稳定性之间的权衡。

### 需要回答的问题

1. 截断 SVD 为什么得到秩不超过 $k$ 的矩阵？
2. 为什么应优先保留最大的奇异值？
3. Eckart–Young 定理的“最佳”针对什么误差度量？
4. 谱范数误差和 Frobenius 范数误差怎样由被丢弃的奇异值计算？
5. 低秩表示为什么能够压缩存储？
6. 截断奇异值为什么可能同时实现去噪和稳定化？

---

## 八、第七步：PCA 与数据的主要变化方向

设数据矩阵：

$$
X\in\mathbb R^{N\times d},
$$

其中每一行是一个样本，每一列是一个特征。

PCA 的第一步必须是中心化：

$$
X_c
=
X-\mathbf 1\mu^{\mathsf T},
$$

其中 $\mu$ 是各特征均值向量。

设 $r=\operatorname{rank}(X_c)$，对中心化数据做紧致 SVD：

$$
X_c=U_r\Sigma_rV_r^{\mathsf T}.
$$

则：

- $V_r$ 的列是数据特征空间中方差非零的主成分方向；
- $X_cV_r=U_r\Sigma_r$ 是各样本的主成分得分；
- 第 $i$ 个主方向的样本方差为 $\sigma_i^2/(N-1)$；
- 前 $k$ 个右奇异向量给出 $k$ 维主成分子空间。

如果补齐特征空间中的正交基，剩余方向都位于 $\mathcal N(X_c)$ 中，其样本方差为零。

样本协方差矩阵为：

$$
C
=
\frac{1}{N-1}X_c^{\mathsf T}X_c.
$$

由 SVD：

$$
C
=
V_r
\frac{\Sigma_r^2}{N-1}
V_r^{\mathsf T}.
$$

所以方差非零的 PCA 主成分方向既是协方差矩阵的特征向量，也是中心化数据矩阵的右奇异向量。

解释方差比为：

$$
\boxed{
\text{EVR}_i
=
\frac{\sigma_i^2}
{\sum_j\sigma_j^2}
}.
$$

使用前 $k$ 个主成分时，降维坐标为：

$$
Z_k=X_cV_k=U_k\Sigma_k,
$$

重建中心化数据为：

$$
\hat X_c
=
Z_kV_k^{\mathsf T}
=
U_k\Sigma_kV_k^{\mathsf T}.
$$

最后加回均值，得到原坐标中的近似数据。

重点学习：

- 为什么 PCA 必须先中心化；
- 主成分方向、得分和重建分别是什么；
- PCA 与协方差矩阵特征分解的关系；
- 奇异值平方为什么对应方差；
- 解释方差比怎样帮助选择维数；
- PCA 为什么本质上是中心化数据的最佳低秩近似；
- 主成分为什么未必具有天然语义；
- 高解释方差为什么不自动保证下游任务效果最好。

### 需要回答的问题

1. 为什么不中心化会让均值位置干扰主方向？
2. PCA 的主成分方向为什么是 $V_r$ 的列而不是 $U_r$ 的列？
3. 主成分得分 $X_cV_k$ 表示什么？
4. 为什么协方差特征值等于 $\sigma_i^2/(N-1)$？
5. 怎样从解释方差比选择保留维数 $k$？
6. PCA 降维与截断 SVD 重建为什么是同一个低秩结构？

---

## 九、第八步：SVD、特征分解、QR 与极分解的统一认识

需要建立以下方法边界。

### 特征分解

研究方阵在自身空间中的不变方向：

$$
Av=\lambda v.
$$

只有拥有足够多特征向量时，才能写成：

$$
A=P\Lambda P^{-1}.
$$

### SVD

研究任意矩阵的输入主方向、输出主方向和非负缩放强度：

$$
Av_i=\sigma_i u_i.
$$

任意实矩阵都存在：

$$
A=U\Sigma V^{\mathsf T}.
$$

### QR 分解

把矩阵列空间写成标准正交基与坐标系数：

$$
A=QR.
$$

它适合高效求解满列秩最小二乘问题，但不会像 SVD 一样直接给出秩亏、全部主伸缩方向和最佳低秩近似。

### 极分解

SVD 还能导出：

$$
A=WH,
$$

其中：

$$
H
=
(A^{\mathsf T}A)^{1/2}
=
V_r\Sigma_rV_r^{\mathsf T}
$$

是 $n\times n$ 的对称半正定伸缩部分。由紧致 SVD 可取：

$$
W=U_rV_r^{\mathsf T},
$$

它是从 $\mathcal R(A^{\mathsf T})$ 到 $\mathcal R(A)$ 的部分等距映射；当 $A$ 是可逆方阵时，$W$ 就是正交矩阵。

极分解把任意线性变换理解为：

$$
\text{先做对称伸缩}
\longrightarrow
\text{再做正交方向变换或部分等距映射}.
$$

重点学习：

- 特征值与奇异值为什么不是同一个概念；
- 对称半正定矩阵的特征分解怎样与 SVD 重合；
- QR 和 SVD 在最小二乘问题中的职责差异；
- 为什么 SVD 比特征分解更适合一般矩形矩阵；
- 为什么 SVD 比 QR 更能暴露数值秩和敏感方向；
- 极分解怎样从 SVD 中得到。

### 需要回答的问题

1. 为什么特征分解只研究方阵，而 SVD 适用于任意矩阵？
2. 奇异值与特征值在符号、空间和几何意义上有什么区别？
3. 实对称半正定矩阵的 SVD 与特征分解怎样对应？
4. QR 与 SVD 分别更适合解决哪些问题？
5. 为什么秩亏或病态最小二乘更适合使用 SVD？
6. 极分解中的对称伸缩部分怎样由奇异值构成？

---

## 十、需要建立的数值计算意识

阶段 6 的理论公式不应被机械理解为大型数值软件的实际算法。

需要理解：

- 手算可以从 $A^{\mathsf T}A$ 求 SVD，但实际计算通常不会显式形成它，因为条件数会被平方；
- 大型矩阵通常使用双对角化、迭代算法或随机化算法计算 SVD；
- 大型稀疏矩阵往往只计算少数最大或最小奇异值；
- 奇异值是否视为零，必须结合矩阵尺度、机器精度和任务容差；
- 不应对极小奇异值无条件取倒数；
- 伪逆是清晰的数学对象，但实际实现通常直接调用稳定的最小二乘或 SVD 求解器；
- 条件数描述问题敏感性，不等于某个算法一定失败；
- 截断 SVD 和正则化会引入偏差，以换取更小的方差和更好的稳定性；
- PCA 对量纲敏感，特征尺度差异很大时应考虑标准化；
- 缺失值、异常值和非线性结构不会被普通 PCA 自动妥善处理；
- 图像或数据压缩的质量不能只由解释方差决定，还应结合实际任务评价。

本阶段手算仍以低维矩阵为主，重点是理解输入方向、输出方向、信息强度和稳定性之间的结构，而不是模仿软件内部的大规模算法。

---

## 十一、推荐学习顺序

按照以下顺序推进：

```text
特征分解的限制
→ AᵀA 的对称半正定结构
→ 奇异值、左奇异向量、右奇异向量
→ 完整、经济型与紧致 SVD
→ SVD 的几何意义与手算
→ 秩和四个基本子空间
→ 外积展开
→ Moore–Penrose 伪逆
→ 投影、最小二乘与最小范数解
→ 矩阵范数、条件数与数值秩
→ 截断 SVD 与最佳低秩近似
→ 压缩、去噪与稳定化
→ PCA、解释方差与重建
→ SVD、特征分解和 QR 的比较
```

暂时不深入：

- SVD 数值算法的完整推导；
- Golub–Kahan 双对角化；
- Lanczos 与 Krylov 子空间方法；
- 随机化 SVD 的误差证明；
- Tikhonov 正则化的完整统计理论；
- 核 PCA 与非线性流形学习；
- 鲁棒 PCA 与稀疏低秩分解；
- 张量分解；
- 大规模推荐系统的工程实现。

---

## 十二、阶段完成标准

阶段 6 完成时，应当能够拿到一个简单矩阵 $A\in\mathbb R^{m\times n}$，独立回答：

1. 为什么特征分解不能统一处理任意矩阵？
2. 为什么 $A^{\mathsf T}A$ 一定对称半正定？
3. 怎样从 $A^{\mathsf T}A$ 求奇异值和右奇异向量？
4. 怎样由非零奇异值得到左奇异向量？
5. 怎样构造并验证 $A=U\Sigma V^{\mathsf T}$？
6. 完整、经济型和紧致 SVD 有什么区别？
7. 怎样从旋转、缩放和椭球解释 SVD？
8. 怎样由 SVD 读出秩和四个基本子空间？
9. 怎样把矩阵展开为若干个秩一外积之和？
10. 怎样由 SVD 构造 Moore–Penrose 伪逆？
11. $AA^+$ 与 $A^+A$ 分别是什么投影？
12. $A^+b$ 在精确解、最小二乘解和最小范数解中扮演什么角色？
13. 为什么最大奇异值等于谱范数？
14. 最小奇异值和条件数怎样描述数值敏感性？
15. 小奇异值为什么会在伪逆中放大噪声？
16. 怎样构造截断 SVD 和最佳秩 $k$ 近似？
17. 怎样由被丢弃的奇异值计算近似误差？
18. 为什么低秩近似能够用于压缩、去噪和稳定化？
19. PCA 为什么必须先中心化？
20. 怎样从数据矩阵 SVD 得到主成分方向、得分、解释方差与重建？
21. SVD、特征分解和 QR 的适用范围有什么区别？
22. 为什么理论上从 $A^{\mathsf T}A$ 出发，并不意味着数值软件应显式形成 $A^{\mathsf T}A$？

完成本阶段后，应形成下面的统一认识：

$$
\boxed{
\begin{aligned}
A=U\Sigma V^{\mathsf T}
&:\text{把任意矩阵分解为输入旋转、正交缩放和输出旋转},\\
\sigma_i
&:\text{测量各主方向的信息强度与数值敏感性},\\
A^+
&:\text{统一精确解、最小二乘解和最小范数解},\\
A_k
&:\text{保留最重要的 }k\text{ 个秩一模式},\\
X_c=U\Sigma V^{\mathsf T}
&:\text{把数据变化分解为主成分方向、得分和方差}.
\end{aligned}
}
$$

阶段 6 完成后，最自然的后续方向是：

$$
\text{正则化与逆问题}
\longrightarrow
\text{概率统计与回归}
\longrightarrow
\text{优化}
\longrightarrow
\text{机器学习中的降维和表示学习}.
$$

因为下一步将进一步研究：

> 当数据带有噪声、模型需要泛化、维度很高或关系不再完全线性时，怎样在拟合能力、稳定性、复杂度和解释性之间取得平衡？
