从知识体系出发，整理重点不再是“我是怎样理解的”，而是回答：

> 线性代数研究哪些对象、概念之间有什么依赖关系，以及整套知识最终怎样汇合。

最适合当前项目的总框架仍然以空间为起点，以线性变换为中心，以 $A\boldsymbol{x}=\boldsymbol{b}$ 为汇合点，再向几何、分解和应用延伸。

# 线性代数知识体系：从空间结构到 $A\boldsymbol{x}=\boldsymbol{b}$

## 记号约定

本文默认在实数域 $\mathbb R$ 上讨论；若改为复数域 $\mathbb C$，涉及内积时应将转置 $A^{\mathsf T}$ 替换为共轭转置 $A^{*}$。

| 对象 | 统一记号 | 示例 |
|---|---|---|
| 标量 | 小写希腊字母或普通小写字母 | $\alpha,\beta,\lambda,x_i$ |
| 向量 | 粗体小写字母 | $\boldsymbol{x},\boldsymbol{v},\boldsymbol{b}$ |
| 矩阵 | 大写字母 | $A,P,Q,U,V$ |
| 向量空间 | 大写斜体字母 | $V,W$ |
| 有序基 | 花体大写字母 | $\mathcal B,\mathcal C$ |
| 零向量 | 粗体零 | $\boldsymbol{0}$ |
| 转置、逆、伪逆 | 规范上标 | $A^{\mathsf T},A^{-1},A^{+}$ |

## 一、整套知识研究的核心对象

线性代数的核心不是单独研究向量、矩阵或方程，而是研究：

> 向量空间中的对象，怎样通过线性规则从一个空间映射到另一个空间。

最一般的表达是：

$$
T:V\longrightarrow W
$$

其中：

- $V$ 是输入空间；
- $W$ 是输出空间；
- $\boldsymbol{v}\in V$ 是输入向量；
- $T$ 是线性变换；
- $T(\boldsymbol{v})\in W$ 是输出向量。

选择输入基 $\mathcal B$ 和输出基 $\mathcal C$ 后，线性变换可以写成矩阵形式：

$$
[T(\boldsymbol{v})]_{\mathcal C}
=
[T]_{\mathcal C\leftarrow\mathcal B}
[\boldsymbol{v}]_{\mathcal B}
=
A[\boldsymbol{v}]_{\mathcal B}
$$

在标准坐标下，通常简写为：

$$
A\boldsymbol{x}=\boldsymbol{b}
$$

因此，整个知识体系可以浓缩为：

```text
输入空间 V
→ 选择输入基 B
→ 输入坐标 x
→ 线性变换 T
→ 矩阵表示 A
→ 输出坐标 b
→ 输出空间 W
```

---

## 二、第一层：向量空间与研究对象

### 1. 标量

标量是用于缩放向量的数，通常来自实数域 $\mathbb R$ 或复数域 $\mathbb C$。

数域决定了：

- 允许使用哪些系数；
- 怎样定义内积；
- 某些特征值是否存在；
- 向量组是否线性相关。

### 2. 向量

向量是向量空间中的元素。

它可以是：

- 几何位移；
- 数值序列；
- 多项式；
- 函数；
- 矩阵；
- 信号；
- 数据样本。

向量的本质不在于外观，而在于它可以进行向量加法和标量乘法。

### 3. 向量空间

向量空间是一个对向量加法和标量乘法封闭，并满足线性运算规律的对象集合。

核心性质是：

$$
\boldsymbol{u},\boldsymbol{v}\in V,
\quad
\alpha,\beta\in\mathbb F
\quad\Longrightarrow\quad
\alpha\boldsymbol{u}+\beta\boldsymbol{v}\in V
$$

### 4. 子空间

子空间是原向量空间中仍然保持向量空间结构的子集合。

判断一个集合是不是子空间，核心检查：

- 是否包含零向量；
- 是否对加法封闭；
- 是否对数乘封闭。

因此，不经过原点的直线和平面通常不是子空间，而是仿射集合。

---

## 三、第二层：线性组合与空间生成

### 1. 线性组合

给定向量：

$$
\boldsymbol{v}_1,\boldsymbol{v}_2,\ldots,\boldsymbol{v}_n
$$

表达式：

$$
x_1\boldsymbol{v}_1+x_2\boldsymbol{v}_2+\cdots+x_n\boldsymbol{v}_n
$$

称为这些向量的线性组合。

线性组合是整个线性代数中最基础的操作。

后面的张成空间、基、矩阵乘法、线性变换和方程组，都建立在线性组合之上。

### 2. 张成空间

所有可能的线性组合构成：

$$
\operatorname{span}\{\boldsymbol{v}_1,\boldsymbol{v}_2,\ldots,\boldsymbol{v}_n\}
$$

张成空间回答：

> 现有向量能够生成哪些对象？

### 3. 线性相关与线性无关

如果：

$$
x_1\boldsymbol{v}_1+x_2\boldsymbol{v}_2+\cdots+x_n\boldsymbol{v}_n
=
\boldsymbol{0}
$$

只有全零解，那么向量组线性无关。

如果存在不全为零的解，那么向量组线性相关。

其结构意义是：

- 线性无关：每个向量都提供新的独立方向；
- 线性相关：向量组中存在可被其他向量代替的冗余方向。

### 4. 基

一组向量成为空间的基，需要同时满足：

$$
\text{基}
\quad\Longleftrightarrow\quad
\text{线性无关}
+
\text{张成整个空间}
$$

基解决两个问题：

- 张成性保证任意向量都能被表示；
- 线性无关保证表示方式唯一。

### 5. 维数

空间的维数等于任意一组基中向量的数量。

因此：

> 维数是描述空间所需的独立方向数量。

---

## 四、第三层：坐标与表示系统

### 1. 坐标

设：

$$
\mathcal B=(\boldsymbol{b}_1,\boldsymbol{b}_2,\ldots,\boldsymbol{b}_n)
$$

是空间的一组有序基。

如果：

$$
\boldsymbol{v}
=
x_1\boldsymbol{b}_1+x_2\boldsymbol{b}_2+\cdots+x_n\boldsymbol{b}_n
$$

那么：

$$
[\boldsymbol{v}]_{\mathcal B}
=
\begin{bmatrix}
x_1\\x_2\\\vdots\\x_n
\end{bmatrix}
$$

是向量 $\boldsymbol{v}$ 在基 $\mathcal B$ 下的坐标。

必须区分：

$\boldsymbol{v}$

和：

$[\boldsymbol{v}]_{\mathcal B}$

### 2. 基矩阵

把基向量按列排列：

$$
P_{\mathcal B}
=
\begin{bmatrix}
\boldsymbol{b}_1&\boldsymbol{b}_2&\cdots&\boldsymbol{b}_n
\end{bmatrix}
$$

则：

$$
\boldsymbol{v}
=
P_{\mathcal B}[\boldsymbol{v}]_{\mathcal B}
$$

表示从 $\mathcal B$ 坐标恢复到标准坐标。

反过来：

$$
[\boldsymbol{v}]_{\mathcal B}
=
P_{\mathcal B}^{-1}\boldsymbol{v}
$$

### 3. 换基

若同一个向量分别用基 $\mathcal B$ 和基 $\mathcal C$ 表示，则：

$$
P_{\mathcal C}[\boldsymbol{v}]_{\mathcal C}
=
P_{\mathcal B}[\boldsymbol{v}]_{\mathcal B}
$$

因此：

$$
[\boldsymbol{v}]_{\mathcal C}
=
P_{\mathcal C}^{-1}P_{\mathcal B}
[\boldsymbol{v}]_{\mathcal B}
$$

换基的本质是：

> 向量不变，描述向量的坐标语言发生改变。

---

## 五、第四层：线性变换与矩阵

### 1. 线性变换

映射：

$$
T:V\longrightarrow W
$$

如果满足：

$$
T(\alpha\boldsymbol{u}+\beta\boldsymbol{v})
=
\alpha T(\boldsymbol{u})+\beta T(\boldsymbol{v})
$$

就是线性变换。

线性变换保持线性组合，因此它一定满足：

$$
T(\boldsymbol{0})=\boldsymbol{0}
$$

### 2. 基向量的像决定整个变换

如果输入空间的一组基为：

$$
\mathcal B=(\boldsymbol{b}_1,\boldsymbol{b}_2,\ldots,\boldsymbol{b}_n)
$$

任意输入都可以写成：

$$
\boldsymbol{v}
=
x_1\boldsymbol{b}_1+x_2\boldsymbol{b}_2+\cdots+x_n\boldsymbol{b}_n
$$

那么：

$$
T(\boldsymbol{v})
=
x_1T(\boldsymbol{b}_1)
+x_2T(\boldsymbol{b}_2)
+\cdots
+x_nT(\boldsymbol{b}_n)
$$

因此，只要知道所有 $T(\boldsymbol{b}_j)$，就确定了整个线性变换。

### 3. 矩阵表示

把 $T(\boldsymbol{b}_j)$ 在输出基 $\mathcal C$ 下的坐标按列排列，得到矩阵 $A$：

$$
A
=
[T]_{\mathcal C\leftarrow\mathcal B}
=
\begin{bmatrix}
[T(\boldsymbol{b}_1)]_{\mathcal C}
&
[T(\boldsymbol{b}_2)]_{\mathcal C}
&\cdots&
[T(\boldsymbol{b}_n)]_{\mathcal C}
\end{bmatrix}
$$

所以：

$$
[T(\boldsymbol{v})]_{\mathcal C}
=
A[\boldsymbol{v}]_{\mathcal B}
$$

矩阵不是线性变换本身，而是线性变换相对于输入基和输出基的数字表示。

### 4. 矩阵尺寸

一个 $m\times n$ 矩阵表示：

$$
A:\mathbb R^n\longrightarrow\mathbb R^m
$$

其中：

- 列数 $n$ 对应输入空间维数；
- 行数 $m$ 对应输出空间维数；
- 第 $j$ 列是第 $j$ 个输入基向量的像；
- 第 $i$ 行负责计算第 $i$ 个输出分量。

---

## 六、第五层：矩阵运算

### 1. 加法、减法与数乘

这些运算表示同类矩阵之间的线性组合：

- 加法：叠加两个变换的输出；
- 减法：比较两个变换或两组数据的差异；
- 数乘：整体缩放一个变换的输出。

### 2. 矩阵乘向量

如果：

$$
A=
\begin{bmatrix}
\boldsymbol{a}_1&\boldsymbol{a}_2&\cdots&\boldsymbol{a}_n
\end{bmatrix}
$$

那么：

$$
A\boldsymbol{x}
=
x_1\boldsymbol{a}_1+x_2\boldsymbol{a}_2+\cdots+x_n\boldsymbol{a}_n
$$

它同时表示：

- 矩阵与向量的计算；
- 矩阵各列的线性组合；
- 输入经过线性变换得到输出；
- 方程组左侧的统一表达。

### 3. 矩阵乘法

如果 $B$ 表示第一个变换，$A$ 表示第二个变换，则：

$$
(AB)\boldsymbol{x}=A(B\boldsymbol{x})
$$

所以 $AB$ 表示：

```text
先进行B，再进行A
```

矩阵乘法表示线性变换的复合，因此一般不满足交换律。

### 4. 转置

转置的表层作用是交换矩阵的行和列。

更深层的关系是：

$$
\langle A\boldsymbol{x},\boldsymbol{y}\rangle
=
\langle\boldsymbol{x},A^{\mathsf T}\boldsymbol{y}\rangle
$$

它说明输出空间中的测量方向 $\boldsymbol{y}$，可以通过 $A^{\mathsf T}$ 拉回输入空间。

因此，转置联系了：

- 行空间与列空间；
- 输入空间与输出空间；
- 内积；
- 正交关系；
- 最小二乘；
- 梯度与反向传播。

转置不是逆矩阵。逆矩阵恢复输入，转置传递测量方式或敏感度。

---

## 七、第六层：$A\boldsymbol{x}=\boldsymbol{b}$ 与方程求解

$A\boldsymbol{x}=\boldsymbol{b}$ 可以从三种语言理解。

### 1. 方程组语言

它表示若干个线性方程构成的系统。

### 2. 列向量语言

它表示：

$$
x_1\boldsymbol{a}_1+x_2\boldsymbol{a}_2+\cdots+x_n\boldsymbol{a}_n
=
\boldsymbol{b}
$$

即判断 $\boldsymbol{b}$ 能否由矩阵的列向量线性表示。

### 3. 线性变换语言

它表示寻找某个输入 $\boldsymbol{x}$，使其经过变换 $A$ 后得到输出 $\boldsymbol{b}$。

---

## 八、第七层：列空间、零空间与秩

### 1. 列空间

$$
\operatorname{Col}(A)
=
\{A\boldsymbol{x}:\boldsymbol{x}\in\mathbb R^n\}
$$

列空间是矩阵所有可能输出的集合。

所以：

$$
A\boldsymbol{x}=\boldsymbol{b}\ 有解
\quad\Longleftrightarrow\quad
\boldsymbol{b}\in\operatorname{Col}(A)
$$

### 2. 零空间

$$
\mathcal N(A)
=
\{\boldsymbol{x}\in\mathbb R^n:A\boldsymbol{x}=\boldsymbol{0}\}
$$

零空间表示被矩阵完全消除的输入方向。

如果 $\boldsymbol{z}$ 属于零空间，则：

$$
A(\boldsymbol{x}+\boldsymbol{z})
=
A\boldsymbol{x},
\qquad
\boldsymbol{z}\in\mathcal N(A)
$$

这说明零空间中的信息无法从输出中恢复。

### 3. 秩

$$
\operatorname{rank}(A)
=
\dim\operatorname{Col}(A)
$$

秩表示矩阵能够产生的独立输出方向数量。

### 4. 秩—零度定理

对于 $m\times n$ 矩阵：

$$
\operatorname{rank}(A)
+
\operatorname{nullity}(A)
=n
$$

它描述输入空间中的方向如何分配：

```text
输入空间
=
有效传递方向
+
被消除方向
```

---

## 九、第八层：高斯消元与解的结构

### 1. 高斯消元

高斯消元通过初等行变换，把方程组转化为更容易读取的等价形式。

它用于识别：

- 主元；
- 自由变量；
- 秩；
- 方程是否一致；
- 解的自由度。

### 2. 三种解的情况

$A\boldsymbol{x}=\boldsymbol{b}$ 可能：

- 无解；
- 有唯一解；
- 有无穷多解。

有解的条件是：

$$
\operatorname{rank}(A)
=
\operatorname{rank}\!\left(
\begin{bmatrix}A&\boldsymbol{b}\end{bmatrix}
\right)
$$

唯一解还要求：

$$
\mathcal N(A)=\{\boldsymbol{0}\}
$$

### 3. 全部解的结构

如果 $\boldsymbol{x}_p$ 是一个特解，那么所有解为：

$$
\boldsymbol{x}
=
\boldsymbol{x}_p+\boldsymbol{x}_n
$$

其中：

$$
\boldsymbol{x}_n\in\mathcal N(A)
$$

所以：

```text
非齐次方程全部解
=
一个特解
+
齐次方程全部解
```

---

## 十、第九层：四个基本子空间

对于 $m\times n$ 矩阵 $A$，存在四个基本子空间。

| 子空间 | 所在空间 | 回答的问题 |
|---|---|---|
| 列空间 $\operatorname{Col}(A)$ | $\mathbb R^m$ | 能产生哪些输出？ |
| 左零空间 $\mathcal N(A^{\mathsf T})$ | $\mathbb R^m$ | 哪些输出方向无法由列空间解释？ |
| 行空间 $\operatorname{Row}(A)$ | $\mathbb R^n$ | 输入中哪些方向会影响输出？ |
| 零空间 $\mathcal N(A)$ | $\mathbb R^n$ | 哪些输入方向会被完全消除？ |

它们构成两组正交补：

$$
\mathbb R^n
=
\operatorname{Row}(A)\oplus\mathcal N(A)
$$

$$
\mathbb R^m
=
\operatorname{Col}(A)\oplus\mathcal N(A^{\mathsf T})
$$

这两组分解分别描述输入空间和输出空间的完整结构。

---

## 十一、第十层：正交、投影与最小二乘

### 1. 内积

内积用于定义：

- 长度；
- 距离；
- 夹角；
- 正交；
- 投影。

标准内积为：

$$
\langle\boldsymbol{u},\boldsymbol{v}\rangle
=
\boldsymbol{u}^{\mathsf T}\boldsymbol{v}
$$

### 2. 正交投影

当 $\boldsymbol{b}\notin\operatorname{Col}(A)$ 时，$A\boldsymbol{x}=\boldsymbol{b}$ 没有精确解。

这时寻找列空间中距离 $\boldsymbol{b}$ 最近的向量：

$$
\boldsymbol{p}=A\widehat{\boldsymbol{x}}
$$

残差：

$$
\boldsymbol{r}=\boldsymbol{b}-\boldsymbol{p}
$$

满足：

$$
\boldsymbol{r}\perp\operatorname{Col}(A)
$$

### 3. 最小二乘

最小二乘问题是：

$$
\widehat{\boldsymbol{x}}
=
\underset{\boldsymbol{x}\in\mathbb R^n}{\operatorname{argmin}}
\,\lVert A\boldsymbol{x}-\boldsymbol{b}\rVert_2^2
$$

由残差正交得到：

$$
A^{\mathsf T}
\left(A\widehat{\boldsymbol{x}}-\boldsymbol{b}\right)
=
\boldsymbol{0}
$$

即正规方程：

$$
A^{\mathsf T}A\widehat{\boldsymbol{x}}
=
A^{\mathsf T}\boldsymbol{b}
$$

因此：

> 最小二乘本质上是把不可到达的目标投影到矩阵的列空间。

### 4. QR分解

$$
A=QR
$$

其中：

- $Q$ 的列是列空间的标准正交基；
- $R$ 记录原列向量在该正交基下的坐标。

QR主要用于：

- 构造正交基；
- 计算投影；
- 稳定求解最小二乘问题。

---

## 十二、第十一层：可逆性、行列式与特征结构

### 1. 可逆性

方阵 $A$ 可逆意味着每个输出都恰好对应一个输入。

它等价于：

$$
\begin{aligned}
\operatorname{rank}(A)&=n,\\
\mathcal N(A)&=\{\boldsymbol{0}\},\\
\operatorname{Col}(A)&=\mathbb R^n,\\
\det(A)&\ne0.
\end{aligned}
$$

### 2. 行列式

行列式描述变换对有向面积或体积的缩放。

- $|\det(A)|$：体积缩放倍数；
- 正负号：空间定向是否翻转；
- $\det(A)=0$：空间被压缩到低维，矩阵不可逆。

### 3. 特征值与特征向量

$$
A\boldsymbol{v}=\lambda\boldsymbol{v},
\qquad
\boldsymbol{v}\ne\boldsymbol{0}
$$

特征向量是经过变换后仍然停留在原方向直线上的非零向量。

特征值表示该方向上的缩放倍数。

### 4. 对角化

如果矩阵具有足够多线性无关特征向量，则：

$$
A=P\Lambda P^{-1}
$$

其含义是：

```text
转换到特征向量坐标
→ 沿各特征方向分别缩放
→ 转回原坐标
```

### 5. 对称矩阵

实对称矩阵满足谱定理：

$$
A=Q\Lambda Q^{\mathsf T}
$$

它具有：

- 实特征值；
- 标准正交特征向量基；
- 正交对角化；
- 清晰的几何主方向。

这进一步连接二次型、正定性、优化和协方差矩阵。

---

## 十三、第十二层：SVD、伪逆与低秩结构

### 1. 奇异值分解

任意 $m\times n$ 矩阵都可以写成：

$$
A=U\Sigma V^{\mathsf T}
$$

几何过程可写为：

$$
\mathbb R^n
\xrightarrow{\;V^{\mathsf T}\;}
\mathbb R^n
\xrightarrow{\;\Sigma\;}
\mathbb R^m
\xrightarrow{\;U\;}
\mathbb R^m
$$

其中：

- $V^{\mathsf T}$ 在输入空间中旋转或反射；
- $\Sigma$ 沿两组正交方向进行缩放，并可能将某些方向压缩为零；
- $U$ 在输出空间中旋转或反射。

### 2. SVD与四个基本子空间

非零奇异值对应有效传递方向。

- $V$ 中对应非零奇异值的方向构成行空间；
- $V$ 中对应零奇异值的方向构成零空间；
- $U$ 中对应非零奇异值的方向构成列空间；
- $U$ 中其余方向构成左零空间。

因此，SVD把秩、四个基本子空间和几何缩放统一起来。

### 3. 伪逆

$$
A^{+}=V\Sigma^{+}U^{\mathsf T}
$$

伪逆用于处理：

- 非方阵；
- 不可逆矩阵；
- 无精确解的方程；
- 解不唯一的问题。

$$
\widehat{\boldsymbol{x}}=A^{+}\boldsymbol{b}
$$

给出最小范数最小二乘解。

### 4. 低秩近似

保留最大的 $k$ 个奇异值：

$$
A_k=U_k\Sigma_kV_k^{\mathsf T}
$$

得到矩阵的最佳秩 $k$ 近似。

它用于：

- 数据压缩；
- 图像压缩；
- 降维；
- 去噪；
- PCA；
- 潜在结构提取。

---

## 十四、完整知识依赖关系

整套知识可以整理成三条主线。

### 主干：空间、表示与变换

```text
标量
→ 向量空间
→ 向量与子空间
→ 线性组合
→ 张成与线性无关
→ 基与维数
→ 坐标与换基
→ 线性变换
→ 矩阵表示
→ 矩阵方程 A x = b
```

### 几何与近似主线

```text
内积
→ 长度与夹角
→ 正交
→ 标准正交基
→ 投影
→ 最小二乘
→ QR
```

### 结构与分解主线

```text
秩与四个基本子空间
→ 可逆性与行列式
→ 特征值与特征向量
→ 对角化
→ 谱定理
→ SVD
→ 伪逆与低秩近似
```

最终，三条主线重新汇合到：

```text
一个线性变换能够产生什么？
会丢失什么？
怎样选择坐标使它更简单？
能否从输出恢复输入？
无法精确恢复时，怎样找到最佳近似？
```

这就是从知识体系出发得到的第二层骨架：以空间和线性变换为核心，$A\boldsymbol{x}=\boldsymbol{b}$ 为中枢，四个基本子空间负责揭示结构，投影与分解负责处理求解、近似和应用。
